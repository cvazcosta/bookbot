# bookbot

BookBot is my first [Boot.dev](https://www.boot.dev) project!

It's a simple program that analyzes a text file and returns some metadata about it, such as the total number of words and the count per character.

## Execution

```ssh
python3 main.py <path_to_book>
```
